/** Backup local deshabilitado — los datos están en Neon (PostgreSQL cloud). */

function startBackupScheduler() {}
function stopBackupScheduler() {}

module.exports = { startBackupScheduler, stopBackupScheduler };
